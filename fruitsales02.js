let fruitInventory = ["Apples", "Peaches", "Pumpkins", "Endives"];
let fruitPrices = [1, 1.5, 7, 12];
let fruitSold = [20, 12, 7, 4];

for (let i = 0; i < fruitInventory.length; i++) {
    console.log(`${fruitInventory[i]}`);
}